

export interface Movie {
    id: number,
    original_title: string,
    overview: string,
    popularity: number,
    image: string,
    release_date: string,
}

export interface CatalogPlaying {
    
}